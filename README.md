# compiler_design
This project is a modular implementation of a language compiler's front-end and back-end phases. It processes a custom subset of programming syntax to demonstrate how source code is transformed into optimized intermediate representations.
